# Client i18n (TASK-142) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Три языка интерфейса (uz/ru/en) для `apps/client` через next-intl + `[locale]`-роутинг, с полной миграцией строк и локализацией контента объявлений через `Accept-Language`.

**Architecture:** next-intl v4 с `localePrefix: 'always'` (`/ru/...`, `/uz/...`, `/en/...`); все роуты переезжают в `src/app/[locale]/`; middleware определяет язык (cookie → Accept-Language → ru); словари `messages/{ru,uz,en}.json`; хелперы форматирования становятся locale-aware; RTK Query и серверные фетчи шлют `Accept-Language`.

**Tech Stack:** Next 15 App Router, React 19, next-intl ^4, RTK Query, pnpm workspace.

**Спек:** `docs/superpowers/specs/2026-06-12-client-i18n-design.md` (утверждён).

**Ветка:** `feat/client-i18n` (уже создана, спек закоммичен). Работаем ТОЛЬКО в `apps/client/` + docs.

**Верификация:** в `apps/client` нет тест-раннера (`"test": "echo \"no tests yet\""`) — по утверждённому спеку ворота качества: `pnpm --filter @avino/client build` зелёный + grep-проверки + ручной прогон трёх локалей. Все bash-команды через `rtk` (`rtk pnpm ...`, `rtk grep ...`).

---

### Task 1: Установка next-intl и i18n-ядро

**Files:**
- Modify: `apps/client/package.json` (через pnpm add)
- Modify: `apps/client/next.config.mjs`
- Create: `apps/client/src/i18n/routing.ts`
- Create: `apps/client/src/i18n/request.ts`
- Create: `apps/client/src/i18n/navigation.ts`
- Create: `apps/client/src/middleware.ts`
- Create: `apps/client/messages/ru.json`, `apps/client/messages/uz.json`, `apps/client/messages/en.json`

- [ ] **Step 1: Установить зависимость**

Run: `rtk pnpm --filter @avino/client add next-intl`
Expected: next-intl ^4 в dependencies, lockfile обновлён.

- [ ] **Step 2: Подключить плагин в next.config.mjs**

```js
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@avino/shared'],
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 3: Создать `src/i18n/routing.ts`**

```ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['uz', 'ru', 'en'],
  defaultLocale: 'ru',
  localePrefix: 'always',
});

export type Locale = (typeof routing.locales)[number];
```

- [ ] **Step 4: Создать `src/i18n/request.ts`**

```ts
import { getRequestConfig } from 'next-intl/server';
import { hasLocale } from 'next-intl';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 5: Создать `src/i18n/navigation.ts`**

```ts
import { createNavigation } from 'next-intl/navigation';
import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

- [ ] **Step 6: Создать `src/middleware.ts`**

```ts
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  // Все маршруты, кроме API, статики Next и файлов с расширением.
  matcher: '/((?!api|_next|_vercel|.*\\..*).*)',
};
```

Детект локали — встроенный в next-intl middleware: cookie `NEXT_LOCALE` → `Accept-Language` → `defaultLocale` (ru). Cookie ставится самим middleware.

- [ ] **Step 7: Создать скелет словарей**

`messages/ru.json` (uz/en — те же ключи, переведённые значения):

```json
{
  "meta": {
    "title": "Avino — недвижимость Узбекистана",
    "description": "Национальный портал недвижимости Узбекистана. Покупка и аренда жилья по всей стране — поиск, фильтры, карта."
  },
  "common": {
    "showMore": "Показать ещё",
    "loading": "Загрузка…",
    "error": "Что-то пошло не так"
  }
}
```

`messages/uz.json`:

```json
{
  "meta": {
    "title": "Avino — O'zbekiston ko'chmas mulki",
    "description": "O'zbekistonning milliy ko'chmas mulk portali. Butun mamlakat bo'ylab uy-joy sotib olish va ijaraga olish — qidiruv, filtrlar, xarita."
  },
  "common": {
    "showMore": "Yana ko'rsatish",
    "loading": "Yuklanmoqda…",
    "error": "Xatolik yuz berdi"
  }
}
```

`messages/en.json`:

```json
{
  "meta": {
    "title": "Avino — Real Estate in Uzbekistan",
    "description": "The national real estate portal of Uzbekistan. Buy and rent property nationwide — search, filters, map."
  },
  "common": {
    "showMore": "Show more",
    "loading": "Loading…",
    "error": "Something went wrong"
  }
}
```

- [ ] **Step 8: Commit**

```bash
git add apps/client/package.json pnpm-lock.yaml apps/client/next.config.mjs apps/client/src/i18n/ apps/client/src/middleware.ts apps/client/messages/
git commit -m "feat(client): add next-intl i18n foundation"
```

---

### Task 2: Переезд роутов в `app/[locale]/` и новый корневой layout

**Files:**
- Move: всё содержимое `apps/client/src/app/*` (кроме `globals.css`) → `apps/client/src/app/[locale]/*`:
  - `page.tsx`, `layout.tsx`, `help/page.tsx`, `search/page.tsx`, `sell/page.tsx`, `sell/new/page.tsx`, `listing/[id]/page.tsx`, `listing/[id]/not-found.tsx`, `account/[tab]/page.tsx`
- Keep: `apps/client/src/app/globals.css` (на месте)
- Rewrite: `apps/client/src/app/[locale]/layout.tsx`

- [ ] **Step 1: Переместить файлы**

```bash
cd apps/client/src/app
mkdir "[locale]"
git mv page.tsx layout.tsx help search sell listing account "[locale]/"
```

- [ ] **Step 2: Переписать `app/[locale]/layout.tsx`**

```tsx
/**
 * Корневой layout публичного портала Avino (под [locale]-префиксом).
 * Inter (latin + cyrillic), next-intl provider, Redux/RTK Provider,
 * chrome (Header + Footer), колоночная раскладка min-h-dvh.
 */
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { notFound } from 'next/navigation';
import { NextIntlClientProvider, hasLocale } from 'next-intl';
import { getTranslations, setRequestLocale } from 'next-intl/server';
import { routing } from '@/i18n/routing';
import { StoreProvider } from '@/store/StoreProvider';
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';
import '../globals.css';

const inter = Inter({
  subsets: ['latin', 'cyrillic'],
  variable: '--font-inter',
  display: 'swap',
});

interface LayoutProps {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export async function generateMetadata({
  params,
}: Pick<LayoutProps, 'params'>): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'meta' });
  return { title: t('title'), description: t('description') };
}

export default async function RootLayout({ children, params }: LayoutProps) {
  const { locale } = await params;
  if (!hasLocale(routing.locales, locale)) notFound();
  setRequestLocale(locale);

  return (
    <html lang={locale} className={inter.variable}>
      <body className="font-sans antialiased">
        <NextIntlClientProvider>
          <StoreProvider>
            <div className="flex min-h-dvh flex-col">
              <Header />
              <main className="flex-1">{children}</main>
              <Footer />
            </div>
          </StoreProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

Статический `export const metadata` удаляется (заменён `generateMetadata`).

- [ ] **Step 3: Прокинуть locale в страницы с params**

В `[locale]/listing/[id]/page.tsx` тип params становится `Promise<{ locale: string; id: string }>`; в `[locale]/account/[tab]/page.tsx` — `{ locale: string; tab: string }` (компонент клиентский — locale приходит из `useLocale()`, params менять по фактическому коду). Серверные страницы без params (`page.tsx`, `search`, `sell`, `help`) получают `params: Promise<{ locale: string }>` там, где locale нужен (металанные/фетчи — Task 5/7+).

- [ ] **Step 4: Сборка**

Run: `rtk pnpm --filter @avino/client build`
Expected: сборка зелёная; роуты в выводе с префиксом `/[locale]/...`.

- [ ] **Step 5: Commit**

```bash
git add -A apps/client/src/app
git commit -m "feat(client): move routes under [locale] prefix"
```

---

### Task 3: Locale-aware навигация во всех компонентах

**Files (Modify):** все импорты `next/link` → `@/i18n/navigation` (Link) и `useRouter`/`usePathname`/`redirect` из `next/navigation` → `@/i18n/navigation`:
- `src/app/[locale]/listing/[id]/page.tsx`, `not-found.tsx`
- `src/app/[locale]/account/[tab]/page.tsx`
- `src/features/home/{Hero,AgentCTA,Districts,Faq,Categories}.tsx`
- `src/features/sell/Sell.tsx`, `src/features/listing-new/ListingNew.tsx`
- `src/features/search/{PropertyCard,FilterBar,SearchResults}.tsx`
- `src/features/detail/{ContactCard,Detail}.tsx`
- `src/features/account/{AccountLayout,Inbox,SavedSearches,MyListings,Notifications,Profile,Favorites}.tsx`
- `src/features/help/Help.tsx`
- `src/components/layout/{Footer,Logo,Header}.tsx`

Правило: `notFound`/`useSearchParams`/`useParams` остаются из `next/navigation`; меняются только `Link`, `useRouter`, `usePathname`, `redirect`. Пути в `href`/`router.push` остаются БЕЗ префикса локали (`/search`) — обёртки добавляют его сами.

- [ ] **Step 1: Найти все вхождения**

Run: `rtk grep -rn "from 'next/link'\|from 'next/navigation'" apps/client/src --include="*.tsx" --include="*.ts"`

- [ ] **Step 2: Заменить импорты по правилу выше**

Пример (`features/home/Hero.tsx`):

```tsx
// было
import Link from 'next/link';
import { useRouter } from 'next/navigation';
// стало
import { Link, useRouter } from '@/i18n/navigation';
```

(`next/link` — default-импорт, `@/i18n/navigation` — именованный.)

- [ ] **Step 3: Сборка**

Run: `rtk pnpm --filter @avino/client build`
Expected: зелёная.

- [ ] **Step 4: Commit**

```bash
git add apps/client/src
git commit -m "refactor(client): use locale-aware navigation wrappers"
```

---

### Task 4: Рабочий LangSwitcher

**Files:**
- Rewrite: `apps/client/src/components/layout/LangSwitcher.tsx`
- Modify: `apps/client/src/components/layout/Header.tsx` (обернуть LangSwitcher в `<Suspense>` — внутри используется `useSearchParams`)

- [ ] **Step 1: Переписать LangSwitcher**

```tsx
/**
 * LangSwitcher — переключатель языка интерфейса.
 * Меняет [locale]-сегмент URL, сохраняя путь и query; cookie NEXT_LOCALE
 * ставит middleware next-intl.
 */
'use client';

import * as React from 'react';
import { Globe, ChevronDown } from 'lucide-react';
import { useLocale } from 'next-intl';
import { useSearchParams } from 'next/navigation';
import { usePathname, useRouter } from '@/i18n/navigation';
import type { Locale } from '@/i18n/routing';
import {
  Dropdown,
  DropdownTrigger,
  DropdownContent,
  DropdownItem,
} from '@/components/ui/dropdown';

const LANGS: { code: Locale; short: string; label: string }[] = [
  { code: 'ru', short: 'RU', label: 'Русский' },
  { code: 'uz', short: 'UZ', label: 'O‘zbekcha' },
  { code: 'en', short: 'EN', label: 'English' },
];

export function LangSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();
  const searchParams = useSearchParams();
  const current = LANGS.find((l) => l.code === locale) ?? LANGS[0];

  function switchTo(next: Locale) {
    if (next === locale) return;
    const qs = searchParams.toString();
    router.replace(qs ? `${pathname}?${qs}` : pathname, { locale: next });
  }

  return (
    <Dropdown>
      <DropdownTrigger asChild>
        <button
          type="button"
          className="flex items-center gap-1 rounded-pill border-[1.5px] border-border px-3 py-[7px] text-[13.5px] font-bold text-ink"
        >
          <Globe size={16} strokeWidth={1.9} /> {current.short}
          <ChevronDown size={14} strokeWidth={2} />
        </button>
      </DropdownTrigger>
      <DropdownContent>
        {LANGS.map((l) => (
          <DropdownItem
            key={l.code}
            selected={l.code === locale}
            onSelect={() => switchTo(l.code)}
          >
            {l.label}
          </DropdownItem>
        ))}
      </DropdownContent>
    </Dropdown>
  );
}
```

Названия языков в LANGS остаются на родном языке каждого (стандарт UX) — в словари не выносятся.

- [ ] **Step 2: Suspense в Header**

В `Header.tsx` место рендера `<LangSwitcher />` обернуть:

```tsx
import { Suspense } from 'react';
...
<Suspense fallback={null}>
  <LangSwitcher />
</Suspense>
```

- [ ] **Step 3: Сборка + ручная проверка**

Run: `rtk pnpm --filter @avino/client build`, затем `rtk pnpm --filter @avino/client dev` и руками: открыть `http://localhost:3001/` → редирект на `/ru`; переключить на UZ на `/ru/search?type=RENT` → URL `/uz/search?type=RENT`; перезагрузка `/` → редирект на `/uz` (cookie).

- [ ] **Step 4: Commit**

```bash
git add apps/client/src/components/layout/LangSwitcher.tsx apps/client/src/components/layout/Header.tsx
git commit -m "feat(client): wire language switcher to locale routing"
```

---

### Task 5: Accept-Language в API-слое + ISO-дата в модели листинга

**Files:**
- Modify: `apps/client/src/store/api/baseQuery.ts`
- Modify: `apps/client/src/lib/api/listings.ts`
- Modify: `apps/client/src/lib/mock/types.ts` (модель Listing)
- Modify: `apps/client/src/lib/format.ts` (удалить `formatRelativeDate`, `isFresh`, `plural`)
- Modify: вызывающие компоненты (`rtk grep -rln "\.created\b\|isFresh\|formatRelativeDate" apps/client/src`)

- [ ] **Step 1: baseQuery — слать Accept-Language**

В `prepareHeaders` (`baseQuery.ts:36`) добавить:

```ts
prepareHeaders: (headers, { getState }) => {
  const token = selectAccessToken(getState() as RootState);
  if (token) {
    headers.set('Authorization', `Bearer ${token}`);
  }
  // Язык интерфейса = язык контента (API.md §1: Accept-Language).
  // <html lang> ставится сервером из [locale]-сегмента — источник надёжный.
  if (typeof document !== 'undefined' && document.documentElement.lang) {
    headers.set('Accept-Language', document.documentElement.lang);
  }
  return headers;
},
```

- [ ] **Step 2: Серверный слой listings — параметр lang**

В `lib/api/listings.ts` каждой экспортируемой функции (`searchListings`, `getListingById`, `getFeaturedListings`, `getSimilarListings` — по фактическому списку) добавить последний параметр `lang: string = 'ru'` и в `fetch` передавать `headers: { 'Accept-Language': lang }`. Вызовы из серверных страниц (`[locale]/page.tsx`, `[locale]/listing/[id]/page.tsx`, `[locale]/search/page.tsx` — по факту) передают `locale` из params.

- [ ] **Step 3: Модель — ISO-дата вместо преформатированной строки**

В `lib/mock/types.ts` у `Listing`: поле `created: string` (преформатированное «2 часа назад») заменить на `createdAt: string` (ISO). В маппере `lib/api/listings.ts` убрать вызов `formatRelativeDate` — писать `createdAt: api.created_at` как есть. В мок-данных `lib/mock/listings.ts` заменить значения на ISO-строки.

- [ ] **Step 4: Рендер относительной даты — next-intl**

Во всех компонентах, рендеривших `listing.created` (найти: `rtk grep -rln "\.created\b" apps/client/src/features apps/client/src/components`):

```tsx
import { useFormatter } from 'next-intl';
...
const format = useFormatter();
...
{format.relativeTime(new Date(listing.createdAt))}
```

Для серверных компонентов — `const format = await getFormatter()` из `next-intl/server`.

`isFresh` (regex по русскому тексту) заменить на дату:

```ts
/** «Новое» объявление: опубликовано менее 3 дней назад. */
export function isFresh(createdAt: string): boolean {
  const then = Date.parse(createdAt);
  if (Number.isNaN(then)) return false;
  return Date.now() - then < 3 * 24 * 60 * 60 * 1000;
}
```

Из `format.ts` удалить `formatRelativeDate` и `plural`.

- [ ] **Step 5: Сборка**

Run: `rtk pnpm --filter @avino/client build`
Expected: зелёная; `rtk grep -rn "formatRelativeDate" apps/client/src` — пусто.

- [ ] **Step 6: Commit**

```bash
git add apps/client/src
git commit -m "feat(client): send Accept-Language and switch listing dates to ISO"
```

---

### Task 6: Locale-aware форматирование (`lib/format.ts`)

**Files:**
- Rewrite: `apps/client/src/lib/format.ts`
- Modify: `apps/client/src/lib/mock/types.ts` (удалить `PROPERTY_TYPE_LABELS`)
- Modify: `messages/{ru,uz,en}.json` — неймспейс `units` и `enums`
- Modify: все вызыватели format-хелперов (`rtk grep -rln "formatPrice\|formatArea\|formatRooms\|formatFloor\|specs(\|txLabel\|propertyTypeLabel\|pinPrice" apps/client/src`)

Паттерн (как ADR-0056 в админке: «хелперы получили параметр locale», здесь — параметр `t`): хелперы принимают translator-функцию неймспейса `units`. Компоненты получают её через `useTranslations('units')` (клиент) или `getTranslations('units')` (сервер).

- [ ] **Step 1: Добавить в словари `units` и `enums`**

`messages/ru.json` (фрагмент; uz/en — те же ключи):

```json
{
  "units": {
    "sum": "сум",
    "perMonth": "/мес",
    "area": "{value} м²",
    "rooms": "{count}-комн.",
    "roomsShort": "{count} комн",
    "floorOf": "{floor}/{total} эт",
    "floor": "{floor} эт",
    "landArea": "{value} м² участок",
    "billion": "млрд",
    "million": "млн"
  },
  "enums": {
    "tx": { "SALE": "Продажа", "RENT": "Аренда" },
    "propertyType": {
      "APARTMENT": "Квартира",
      "HOUSE": "Дом",
      "COMMERCIAL": "Коммерческая",
      "LAND": "Участок"
    }
  }
}
```

(Точные ключи `propertyType` взять из `PROPERTY_TYPE_LABELS` в `lib/mock/types.ts`; RU-значения переносятся как есть.)

`uz`: `"sum": "so'm"`, `"perMonth": "/oy"`, `"area": "{value} m²"`, `"rooms": "{count} xonali"`, `"floorOf": "{floor}/{total}-qavat"`, `"tx": {"SALE": "Sotuv", "RENT": "Ijara"}` и т.д.
`en`: `"sum": "UZS"`, `"perMonth": "/mo"`, `"area": "{value} m²"`, `"rooms": "{count} rm"`, `"floorOf": "fl {floor}/{total}"`, `"tx": {"SALE": "Sale", "RENT": "Rent"}` и т.д.

- [ ] **Step 2: Переписать format.ts на параметр `t`**

```ts
/**
 * Хелперы форматирования. Все подписи/единицы — через словарь (`units`,
 * `enums`): хелперы принимают t-функцию от useTranslations()/getTranslations().
 * Числа — Intl.NumberFormat (пробелы как разделители тысяч).
 */
import type { Currency, Listing, PropertyType, TransactionType } from './mock/types';

/** Translator неймспейса (useTranslations('units') | getTranslations('units')). */
export type T = (key: string, values?: Record<string, unknown>) => string;

const nf = new Intl.NumberFormat('ru-RU');

export interface FormatPriceOptions {
  suffix?: boolean;
}

export function formatPrice(
  listing: Pick<Listing, 'price' | 'currency' | 'tx'>,
  t: T,
  opts: FormatPriceOptions = {},
): string {
  const n = Number(listing.price);
  const isUSD = listing.currency === 'USD';
  const body = isUSD ? '$' + nf.format(n) : nf.format(n) + ' ' + t('sum');
  if (opts.suffix === false) return body;
  return listing.tx === 'RENT' ? body + t('perMonth') : body;
}

export function formatMoney(value: number | string, currency: Currency, t: T): string {
  const n = Number(value);
  return currency === 'USD' ? '$' + nf.format(n) : nf.format(n) + ' ' + t('sum');
}

export function pinPrice(listing: Pick<Listing, 'price' | 'currency'>, t: T): string {
  const n = Number(listing.price);
  const isUSD = listing.currency === 'USD';
  if (isUSD) {
    if (n >= 1000) return '$' + trim(n / 1000) + 'K';
    return '$' + trim(n);
  }
  if (n >= 1e9) return trim(n / 1e9) + ' ' + t('billion');
  if (n >= 1e6) return trim(n / 1e6) + ' ' + t('million');
  if (n >= 1e3) return trim(n / 1e3) + 'K';
  return trim(n);
}

function trim(v: number): string {
  return (Math.round(v * 10) / 10).toString().replace('.', ',');
}

export function formatArea(area: string | number | undefined, t: T): string {
  if (area == null || area === '') return '';
  return t('area', { value: area });
}

export function formatRooms(rooms: number | undefined, t: T): string {
  if (!rooms) return '';
  return t('rooms', { count: rooms });
}

export function formatFloor(floor: number | undefined, totalFloors: number | undefined, t: T): string {
  if (floor && totalFloors) return t('floorOf', { floor, total: totalFloors });
  if (floor) return t('floor', { floor });
  return '';
}

export function specs(
  l: Pick<Listing, 'rooms' | 'area' | 'floor' | 'totalFloors' | 'type'>,
  t: T,
): string[] {
  const parts: string[] = [];
  if (l.rooms) parts.push(t('roomsShort', { count: l.rooms }));
  if (l.area) parts.push(t('area', { value: l.area }));
  if (l.floor && l.totalFloors) parts.push(t('floorOf', { floor: l.floor, total: l.totalFloors }));
  else if (l.type === 'LAND' && l.area) parts.push(t('landArea', { value: l.area }));
  return parts;
}

/** t — от useTranslations('enums') / getTranslations('enums'). */
export function txLabel(tx: TransactionType, t: T): string {
  return t(`tx.${tx}`);
}

export function propertyTypeLabel(type: PropertyType, t: T): string {
  return t(`propertyType.${type}`);
}

/** «Новое» объявление: опубликовано менее 3 дней назад. */
export function isFresh(createdAt: string): boolean {
  const then = Date.parse(createdAt);
  if (Number.isNaN(then)) return false;
  return Date.now() - then < 3 * 24 * 60 * 60 * 1000;
}
```

`PROPERTY_TYPE_LABELS` удалить из `lib/mock/types.ts`; всех потребителей перевести на `propertyTypeLabel(type, t)`.

- [ ] **Step 3: Обновить вызывателей**

Найти: `rtk grep -rln "formatPrice\|formatMoney\|pinPrice\|formatArea\|formatRooms\|formatFloor\|specs(\|txLabel\|propertyTypeLabel\|PROPERTY_TYPE_LABELS" apps/client/src` — в каждом клиентском компоненте добавить `const tUnits = useTranslations('units');` (и `tEnums` где нужно) и прокинуть в хелперы. Серверные (`listing/[id]/page.tsx` generateMetadata) — `await getTranslations({ locale, namespace: 'units' })`.

- [ ] **Step 4: Сборка**

Run: `rtk pnpm --filter @avino/client build`
Expected: зелёная; `rtk grep -rn "PROPERTY_TYPE_LABELS" apps/client/src` — пусто.

- [ ] **Step 5: Commit**

```bash
git add apps/client/src apps/client/messages
git commit -m "refactor(client): locale-aware formatting helpers"
```

---

### Tasks 7–12: Миграция строк по разделам

Общий рецепт для каждого раздела (одинаковый, ниже только списки файлов и неймспейсы):

1. Прочитать файлы раздела, выписать ВСЕ user-facing строки (включая `aria-label`, `placeholder`, `title`, `alt`, тексты кнопок/тостов/empty-state).
2. Добавить ключи в `messages/ru.json` (значения — текущие строки as is), перевести в `uz.json` (латиница) и `en.json`. Ключи — camelCase, по смыслу: `search.filters.priceFrom`, `account.favorites.emptyTitle`.
3. В клиентских компонентах: `const t = useTranslations('<ns>');` → `{t('key')}`; в серверных: `const t = await getTranslations('<ns>');` (или с `{ locale, namespace }` в generateMetadata).
4. Проверка раздела: `rtk grep -rnP "[А-Яа-яЁё]" <файлы раздела>` — пусто (кириллицы в коде не осталось).
5. `rtk pnpm --filter @avino/client build` — зелёная.
6. Commit.

Пример миграции (шаблон):

```tsx
// было
<button className="...">Показать ещё</button>
// стало
const t = useTranslations('search');
<button className="...">{t('showMore')}</button>
```

НЕ переводим (фиксировано спеком): мок-данные контента (`lib/mock/agents.ts`, `lib/mock/districts.ts`, `lib/mock/listings.ts` — имена районов/агентов это данные, ждут geo-reference API), названия языков в LangSwitcher, бренд «Avino».

### Task 7: Layout chrome + auth

**Files:** `components/layout/{Header,Nav,Footer,LoginModal,Logo}.tsx`, `components/SessionBootstrap.tsx`, `store/api/apiError.ts` (сообщения ошибок → ключи `errors.*`; функции дать параметр `t` по паттерну Task 6)
**Неймспейсы:** `nav`, `footer`, `auth`, `errors`
**Commit:** `feat(client): localize layout chrome and auth`

### Task 8: Главная

**Files:** `app/[locale]/page.tsx`, `features/home/{Hero,Categories,Districts,FeaturedCarousel,Agents,AgentCTA,Faq}.tsx`, `components/ui/section-title.tsx` (если есть строки)
**Неймспейс:** `home`
**Commit:** `feat(client): localize home page`

### Task 9: Поиск

**Files:** `app/[locale]/search/page.tsx`, `features/search/{FilterBar,SearchResults,PropertyCard,PropertyCardSkeleton,MapView}.tsx`, `components/ui/{empty-state,fav-button,promo-badge,segment,pill}.tsx`, `lib/savedSearch.ts` (строки, если есть)
**Неймспейсы:** `search`, `common`
**Commit:** `feat(client): localize search page`

### Task 10: Карточка объекта

**Files:** `app/[locale]/listing/[id]/{page,not-found}.tsx` (generateMetadata — locale-aware: `getTranslations({ locale, namespace: 'listing' })`), `features/detail/{Detail,Facts,ContactCard}.tsx`, `components/ui/{gallery,lightbox,photo-img,badge}.tsx`
**Неймспейс:** `listing`
**Commit:** `feat(client): localize listing detail`

### Task 11: Продажа + создание объявления

**Files:** `app/[locale]/sell/{page.tsx,new/page.tsx}`, `features/sell/{Sell,SellFaq}.tsx`, `features/listing-new/{ListingNew,PhotoUploader,PickMap,Progress}.tsx`
**Неймспейсы:** `sell`, `listingNew`
**Commit:** `feat(client): localize sell and listing creation pages`

### Task 12: Помощь + кабинет

**Files:** `app/[locale]/help/page.tsx`, `features/help/{Help,Faq}.tsx`, `app/[locale]/account/[tab]/page.tsx`, `features/account/{AccountLayout,Favorites,Inbox,MyListings,Notifications,Profile,SavedSearches,Settings}.tsx`
**Неймспейсы:** `help`, `account`
**Commit:** `feat(client): localize help and account pages`

---

### Task 13: Финальная зачистка и smoke-прогон

- [ ] **Step 1: Полный grep кириллицы**

Run: `rtk grep -rnP "[А-Яа-яЁё]" apps/client/src --include="*.tsx" --include="*.ts" | rtk grep -v "lib/mock/\|^\s*\*\|^\s*//"`
Expected: только комментарии и мок-данные. Любая user-facing строка — донести в словари.

- [ ] **Step 2: Сборка**

Run: `rtk pnpm --filter @avino/client build`
Expected: зелёная.

- [ ] **Step 3: Ручной smoke (dev-сервер, 3 локали)**

`rtk pnpm --filter @avino/client dev`, проверить на `ru`/`uz`/`en`:
- `/` → редирект по Accept-Language; cookie переживает перезагрузку;
- 7 страниц рендерятся без сырых ключей (`home.`, `search.` в UI = баг);
- переключатель сохраняет путь + query;
- `/de/anything` → 404;
- запросы RTK Query уходят с `Accept-Language` (вкладка Network);
- listing detail title/description в `<head>` локализованы.

- [ ] **Step 4: Commit (если были фиксы)**

```bash
git add -A apps/client
git commit -m "chore(client): i18n cleanup sweep"
```

---

### Task 14: Документация (в этом же PR — правило «Finalize in feature PR»)

- [ ] **Step 1: ADR**

Определить номер: `rtk proxy ls docs/adr | sort | tail -1` → следующий (ожидаемо ADR-0063). Создать `docs/adr/ADR-00XX-client-i18n-next-intl.md` по шаблону CLAUDE.md: контекст (TASK-142, публичный портал vs ADR-0056), решение (next-intl v4, `[locale]` always-prefix, default ru, cookie+Accept-Language detection, словари messages/, Accept-Language в API-слой, locale-aware format-хелперы), последствия (плюсы: SSR на нужном языке, SEO-задел под TASK-183, ICU; минусы: +1 зависимость, URL меняются — все ссылки через обёртки, uz-переводы первичного качества).

- [ ] **Step 2: TASKS.md + DONE.md**

Из `docs/TASKS.md` удалить TASK-142 (раздел M14, строки ~489–535), оставив маркер `_TASK-142 completed — see docs/DONE.md._`. В `docs/DONE.md` добавить запись по формату (Status: DONE, Branch: feat/client-i18n, PR: pending, Files changed, Summary, Commits, Related ADR).

- [ ] **Step 3: Commit**

```bash
git add docs/
git commit -m "docs(client): record TASK-142 client i18n (ADR, TASKS, DONE)"
```

---

### Task 15: Push + PR

- [ ] **Step 1: Проверить авторизацию gh** (`gh auth status`; если не залогинен — `gh auth login --with-token < ~/.gh_token`, токен не печатать)

- [ ] **Step 2: Push**

```bash
git push -u origin feat/client-i18n
```

- [ ] **Step 3: PR**

```text
PR title:
feat(client): i18n foundation with next-intl (uz/ru/en) — TASK-142

PR description:
- next-intl v4 + [locale]-роутинг (/ru, /uz, /en; localePrefix always, default ru)
- middleware: cookie NEXT_LOCALE → Accept-Language → ru; рабочий LangSwitcher с персистентностью
- все роуты перенесены в app/[locale]/, навигация через locale-aware обёртки
- словари messages/{ru,uz,en}.json, полная миграция строк всех страниц
- locale-aware форматирование (units/enums через ICU), даты — useFormatter
- Accept-Language в RTK Query и серверных фетчах — контент объявлений следует за языком UI
- docs: ADR-00XX, TASK-142 → DONE.md

Как проверить:
- pnpm --filter @avino/client build
- dev на :3001 — см. smoke-чеклист в docs/superpowers/plans/2026-06-12-client-i18n.md (Task 13)
```

F) Pre-merge checklist:
- build зелёный
- 7 страниц × 3 локали без сырых ключей
- переключатель + cookie работают
- Accept-Language уходит в API
- изменения только в apps/client + docs
