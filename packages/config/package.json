{
  "name": "@avino/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared build/tooling configuration for the Avino monorepo (base tsconfig, prettier preset).",
  "files": [
    "tsconfig.base.json",
    "prettier-preset.cjs"
  ],
  "exports": {
    "./tsconfig.base.json": "./tsconfig.base.json",
    "./prettier-preset": "./prettier-preset.cjs"
  },
  "scripts": {
    "lint": "echo \"no sources to typecheck\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
